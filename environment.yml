name: bio-compose-server
channels:
  - defaults
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
  - readdy
  - conda-forge
dependencies:
  - pip
  - python=3.10
  # - readdy
  - pymem3dg
  # pip install here to specify simulators, the rest are in pyproject.toml ("pure" python)
  - pip:
    - copasi-basico
    - tellurium
    - python-libsbml
    - smoldyn
