name: compose-server
channels:
  - defaults
  - https://repo.anaconda.com/pkgs/main
  - https://repo.anaconda.com/pkgs/r
  - readdy
  - conda-forge
dependencies:
  - pip
  - python=3.10
  # - readdy
  - pymem3dg
  - pip:  # only simulator-level deps(C compat)
    - simulariumio
    - smoldyn
    - copasi-basico
    - tellurium
    - numpy
    - pandas
    - process-bigraph
    - bigraph-schema